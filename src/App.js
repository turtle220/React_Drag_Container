import React from 'react';
import DragDrop from './DragDrop/DragDrop'

export default function App() {
  // Declare a new state variable, which we'll call "count"

  return (
    <div>
      <DragDrop props='This is test props ***'/>
    </div>
  );
}